# Copy to .env and fill in. .env is gitignored.

# Login email used by tests/test-product.spec.ts (the "Iniciar sesión con correo" step).
# Recorded as a literal value; parameterized here so real data is not committed.
TEST_EMAIL=
